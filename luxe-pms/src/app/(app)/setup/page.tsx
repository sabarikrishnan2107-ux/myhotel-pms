import { SetupView } from "./setup-view";

// Unified configuration hub — Master Setup and the old Settings live here as one
// page with a single grouped sidebar (Personal · Property · Inventory · …).
export default function ConfigPage() {
  return <SetupView />;
}
