# ============================================================================
#  myhotel PMS — FRONTEND (Next.js) production env template
#  Copy to `.env.production` (or set in your host's env vars) before building.
#  This is baked in at BUILD time, so you must rebuild if you change it.
# ============================================================================

# The public URL of your backend API, including the /api suffix.
# Must be https and reachable from your users' browsers.
NEXT_PUBLIC_API_URL=https://api.yourhotel.com/api    # <-- CHANGE
